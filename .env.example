# Public frontend configuration only. All VITE_* values are bundled into the browser.
# Stage 1 supports mock mode only. No credentials are needed.
VITE_SUPPORT_MODE=mock
# Stage 2 will add a server endpoint adapter. Never add provider or database keys here.
