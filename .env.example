# Local frontend-only preview. Production Cloudflare Pages uses live.
VITE_SUPPORT_MODE=mock

# SERVER / ingestion CLI ONLY. Never prefix secrets with VITE_.
# Use Cloudflare Worker encrypted secrets for the deployed values.
RAG_BASE_URL=https://ai-support-rag-demo.pages.dev
RAG_ADMIN_TOKEN=
SUPABASE_URL=
SUPABASE_SECRET_KEY=
OPENROUTER_API_KEY=
SESSION_PEPPER=
RAG_NOTIFICATION_SECRET=
# Server config (worker/wrangler.jsonc): OPENROUTER_MODEL=:free,
# LLM_ENABLED=true, LLM_DAILY_LIMIT=20, SIMILARITY_THRESHOLD=0.66
# Telegram credentials live only in the separate n8n workflow.
